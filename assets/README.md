# assets

ใส่ไฟล์ภาพของเว็บไว้ในโฟลเดอร์นี้

- scenes/ = ภาพฉากหลังหลัก
- characters/ = ตัวละครและ pose ต่าง ๆ
- props/ = ของตกแต่งเสริม
- _source/ = ไฟล์ต้นฉบับ เช่น Figma, PSD, prompt notes

ถ้าเปลี่ยนชื่อไฟล์หรือ path ให้แก้ใน `config.js` ด้วย
